# jpa Setting
spring:
  jpa:
    hibernate:
      ddl-auto: update # jpa DB table update
    generate-ddl: false # db ??? ?? ?
    show-sql: true # ??? sql ??
  # mariaDB Setting
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/ssafy
    username: root
    password: 170318

# Hibernate loggin
logging:
  level:
    org:
      Hibernate: info
