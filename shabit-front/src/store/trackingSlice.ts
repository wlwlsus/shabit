import { createSlice } from '@reduxjs/toolkit';

const trackingSlice = createSlice({
  name: 'trackingSlice',
  initialState: {
  },
  reducers: {
   
  },
});

export default trackingSlice;
// export const { setPose } = trackingSlice.actions;

