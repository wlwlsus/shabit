import { postData } from './post';
import {
  fetchDaily,
  fetchWeekly,
  fetchMonthly,
  fetchHeatmap,
  fetchQuote,
} from './get';
const Stat = {
  fetchDaily,
  fetchWeekly,
  fetchMonthly,
  fetchHeatmap,
  fetchQuote,
  postData,
};

export default Stat;
