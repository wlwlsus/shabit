import { postData } from './post';
import { fetchDaily, fetchWeekly, fetchMonthly, fetchHeatmap } from './get';
const Stat = {
  fetchDaily,
  fetchWeekly,
  fetchMonthly,
  fetchHeatmap,
  postData,
};

export default Stat;
